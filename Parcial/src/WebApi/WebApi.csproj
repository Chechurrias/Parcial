// En Startup.cs o donde configures DI:
using Application.Interfaces;
using Infrastructure.Logging;
using Infrastructure.Data; // Supón que tu implementación de IDatabase está aquí

public void ConfigureServices(IServiceCollection services)
{
    services.AddScoped<ILogger, Logger>();
    services.AddScoped<IDatabase, Database>();
    // Lo mismo para IOrderService si lo implementas en Application o Infrastructure según reglas de tu negocio
}
